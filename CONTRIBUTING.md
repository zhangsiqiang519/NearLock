# NearLock 开发规则

本文档定义 NearLock 项目的开发约定，所有协作者（含 AI 助手）都需遵守。

## 1 语言规范

- 所有沟通、说明、回答、建议、反馈一律使用**中文**。
- 文档（README、docs/、各类 `*.md`）使用中文撰写。
- 代码注释推荐使用中文。
- Git Commit Message **必须**使用中文。

## 2 代码提交规范（核心）

> 本项目的核心约定：**每次改动完成后都要进行一次代码提交。**

### 2.1 提交时机

- 每完成一个**逻辑上完整、可描述**的改动，就提交一次。
- 不把多个不相关的改动堆在一个提交里；也不把一个完整改动拆成无意义的碎提交。
- 一个 commit 应当是自洽的：单独看这次提交能说清「改了什么、为什么改」。

### 2.2 提交信息格式

采用「类型: 简短描述」的中文格式：

```
feat: macOS 端新增 RSSI 阈值设置项
fix: 修复 Android 前台服务被系统回收后广播中断的问题
refactor: 抽取 BLE 扫描逻辑到独立的 Scanner 类
docs: 补充双端共享 Service UUID 说明
chore: 初始化项目结构与开发规则
```

常用类型：

| 类型 | 含义 |
| --- | --- |
| `feat` | 新增功能 |
| `fix` | 修复缺陷 |
| `refactor` | 重构（不改变外部行为） |
| `docs` | 文档变更 |
| `chore` | 构建、配置、杂项 |
| `style` | 格式调整（不影响逻辑） |
| `test` | 测试相关 |

### 2.3 提交操作约定

- 提交前先 `git status` / `git diff` 确认改动范围。
- 优先按文件名精确 `git add <文件>`，避免 `git add .` 误带无关文件。
- 不提交敏感文件：签名密钥（`*.jks` / `*.keystore`）、`local.properties`、`.env` 等已在 `.gitignore` 中排除。
- 默认在 `main` 分支开发；如需较大特性可另开分支，完成后合并。
- 不强制推送、不 `reset --hard` 已共享的提交。

## 3 文档同步规范

每次执行代码相关操作后，需同步更新相关文档，保持文档与代码行为一致：

| 操作类型 | 需更新的文档 |
| --- | --- |
| 新增功能 / 接口 | README 功能说明、使用方式、参数说明 |
| 代码修改 | README 影响范围、使用注意事项 |
| 依赖变更 | README 安装方式与版本要求 |
| 配置 / 架构调整 | README 配置项说明、目录结构 |

未完成文档更新视为任务未完成，禁止只改代码不更新文档。

## 4 双端约定

- macOS 与 Android 共享的常量（如固定 Service UUID）统一在 `shared/` 中记录，两端取值必须一致，改动时同步更新两端。
- macOS 端遵循 Swift / SwiftUI 官方风格；Android 端遵循 Kotlin 官方编码规范与 Jetpack Compose 最佳实践。

## 5 详细编码规范

更细粒度的分层编码规范（后端 / 前端 / 跨层）维护在 `.trellis/spec/` 下，由 Trellis 工作流在编码前自动加载。
